% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_context_keys.R
\name{missing_context_keys}
\alias{missing_context_keys}
\title{Identify linked keys missing from contextual data}
\usage{
missing_context_keys(.data, context, by = "tract_geoid")
}
\arguments{
\item{.data}{A data frame containing linked individual-level records.}

\item{context}{A data frame containing contextual variables.}

\item{by}{Join key specification. A single character string compares columns
with the same name in \code{.data} and \code{context}. A named character vector of
length one compares different column names, where the name is the column in
\code{.data} and the value is the column in \code{context}, for example
\code{c("tract_geoid" = "GEOID")}.}
}
\value{
A tibble with one column containing unique keys from \code{.data} that are
not present in \code{context}.
}
\description{
\code{missing_context_keys()} returns the unique non-missing join keys from
linked individual-level records that are not present in a contextual dataset.
}
\details{
This is useful before or after using \code{\link[=join_context]{join_context()}} to identify geographic
units that need contextual data.
}
\examples{
linked <- tibble::tibble(
  id = 1:3,
  tract_geoid = c("11001980000", "24510040100", "99999999999")
)

context <- tibble::tibble(
  tract_geoid = c("11001980000", "24510040100"),
  deprivation_index = c(0.8, 0.6)
)

missing_context_keys(linked, context)

}
\seealso{
\code{\link[=join_context]{join_context()}}, \code{\link[=context_failures]{context_failures()}}, \code{\link[=context_summary]{context_summary()}}
}

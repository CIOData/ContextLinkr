% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_context.R
\name{join_context}
\alias{join_context}
\title{Join linked records to contextual data}
\usage{
join_context(.data, context, by = "tract_geoid", suffix = c("", "_context"))
}
\arguments{
\item{.data}{A data frame containing linked individual-level records.}

\item{context}{A data frame containing contextual variables.}

\item{by}{Character string giving the join key. Defaults to \code{"tract_geoid"}.}

\item{suffix}{Character vector of length 2 used to disambiguate duplicate
non-key column names in \code{.data} and \code{context}.}
}
\value{
A tibble containing \code{.data} with matching contextual variables
joined from \code{context} and a \code{.context_joined} column indicating whether
each row matched a contextual record.
}
\description{
\code{join_context()} joins individual-level records that have been linked to
Census tract geography with a user-supplied contextual dataset.
}
\details{
This function does not download contextual data. It is intended for workflows
where contextual variables are already available, including future Cancer
InFocus exports or other tract-level datasets.

Use \code{\link[=link_summary]{link_summary()}} after joining to summarize how many linked records
matched contextual data.
}
\examples{
linked <- tibble::tibble(
  id = 1:2,
  tract_geoid = c("11001980000", "24510040100")
)

context <- tibble::tibble(
  tract_geoid = c("11001980000", "24510040100"),
  deprivation_index = c(0.8, 0.6)
)

join_context(linked, context)

}
\seealso{
\code{\link[=link_context]{link_context()}}, \code{\link[=link_summary]{link_summary()}}, \code{\link[=link_successes]{link_successes()}},
\code{\link[=link_failures]{link_failures()}}
}

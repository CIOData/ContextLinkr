% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_context.R
\name{link_context}
\alias{link_context}
\title{Link individual records to Census tract geography}
\usage{
link_context(
  .data,
  address = NULL,
  street = NULL,
  city = NULL,
  state_col = NULL,
  zip = NULL,
  lat = NULL,
  lon = NULL,
  state = NULL,
  geocoder = c("census_batch", "census_single", "osm"),
  confirm_external = FALSE,
  year = 2023,
  keep_geometry = FALSE,
  context_measures = NULL,
  context_format = "wide",
  include_context = FALSE,
  context_cache = TRUE,
  context_refresh_cache = FALSE,
  cache = TRUE
)
}
\arguments{
\item{.data}{A data frame containing individual-level records.}

\item{address}{Optional full address column. Supports quoted or unquoted
column names.}

\item{street}{Optional street address column. Supports quoted or unquoted
column names.}

\item{city}{Optional city column. Supports quoted or unquoted column names.}

\item{state_col}{Optional state column for component address geocoding.
Supports quoted or unquoted column names.}

\item{zip}{Optional ZIP code column. Supports quoted or unquoted column names.}

\item{lat}{Optional latitude column. If both \code{lat} and \code{lon} are supplied,
geocoding is skipped.}

\item{lon}{Optional longitude column. If both \code{lat} and \code{lon} are supplied,
geocoding is skipped.}

\item{state}{Optional state abbreviation, name, FIPS code, or vector of states
used to limit Census tract lookup. If omitted in coordinate-based
workflows, ContextLinkr attempts to infer state from latitude and longitude
using state polygons from \code{\link[tigris:states]{tigris::states()}}. If omitted in address-based
workflows, ContextLinkr uses geocoded state information when available.
Providing \code{state} explicitly overrides inferred state values.}

\item{geocoder}{Geocoder passed to \code{\link[=gc_address]{gc_address()}} when geocoding is needed.}

\item{confirm_external}{Logical. Must be \code{TRUE} before address data are sent
to an external geocoding service.}

\item{year}{Census tract boundary year passed to \code{\link[=id_tract]{id_tract()}}.}

\item{keep_geometry}{Logical. Whether to keep tract geometry in the output.
Passed to \code{\link[=id_tract]{id_tract()}}.}

\item{context_measures}{Optional character vector of Cancer InFocus measure
definitions to retrieve when \code{include_context = TRUE}. If \code{NULL}, all
available measures may be retrieved.}

\item{context_format}{Output format requested from \code{\link[=get_context]{get_context()}} when
\code{include_context = TRUE}. Defaults to \code{"wide"} because linked
individual-level records generally require one row per geography for
joining.}

\item{include_context}{Logical. If \code{TRUE}, retrieve Cancer InFocus
contextual variables for successfully linked Census tracts and join them
back to the linked records. Defaults to \code{FALSE}.}

\item{context_cache}{Logical. If \code{TRUE}, hosted Cancer InFocus context files
are cached locally before reading when \code{include_context = TRUE}.}

\item{context_refresh_cache}{Logical. If \code{TRUE}, hosted Cancer InFocus
context files are downloaded again even when cached copies exist. Ignored
when \code{context_cache = FALSE}.}

\item{cache}{Logical. Whether to use tigris caching during tract lookup.
Passed to \code{\link[=id_tract]{id_tract()}}.}
}
\value{
A tibble containing the original records plus geocoding and/or
Census tract fields. If \code{include_context = TRUE}, the output also includes
selected Cancer InFocus contextual variables and context-join metadata.
}
\description{
\code{link_context()} is an end-to-end convenience wrapper for linking
individual-level records to Census tract geography. It can either use
existing latitude/longitude columns or geocode address fields before
identifying Census tracts.
}
\details{
By default, this function returns the input data with geocoding metadata,
when geocoding is used, and tract identification fields from \code{\link[=id_tract]{id_tract()}}.
If \code{include_context = TRUE}, it retrieves selected Cancer InFocus contextual
variables and joins them to the linked records.

Use \code{\link[=link_summary]{link_summary()}}, \code{\link[=link_successes]{link_successes()}}, and \code{\link[=link_failures]{link_failures()}} to review
linked output after geocoding and tract identification.
}
\examples{
\dontrun{
link_context(
  sample_addresses,
  address = address,
  geocoder = "census_single",
  confirm_external = TRUE
)

link_context(
  geocoded_data,
  lat = latitude,
  lon = longitude
)

link_context(
  geocoded_data,
  lat = latitude,
  lon = longitude,
  state = "KY"
)
}

}
\seealso{
\code{\link[=gc_address]{gc_address()}}, \code{\link[=id_tract]{id_tract()}}, \code{\link[=link_summary]{link_summary()}},
\code{\link[=link_successes]{link_successes()}}, \code{\link[=link_failures]{link_failures()}}
}

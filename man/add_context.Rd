% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_context.R
\name{add_context}
\alias{add_context}
\title{Add Cancer InFocus contextual variables to linked records}
\usage{
add_context(
  .data,
  tract_col = "tract_geoid",
  measures = NULL,
  use_cache = TRUE,
  refresh_cache = FALSE
)
}
\arguments{
\item{.data}{A data frame containing linked individual-level records.}

\item{tract_col}{Column containing Census tract GEOIDs. Supports quoted or
unquoted column names. Defaults to \code{"tract_geoid"}.}

\item{measures}{Optional character vector of Cancer InFocus measure
definitions to retrieve. If \code{NULL}, all available measures may be
retrieved.}

\item{use_cache}{Logical. If \code{TRUE}, hosted Cancer InFocus context files are
cached locally before reading.}

\item{refresh_cache}{Logical. If \code{TRUE}, hosted files are downloaded again
even when cached copies exist. Ignored when \code{use_cache = FALSE}.}
}
\value{
A tibble containing \code{.data} with selected Cancer InFocus contextual
variables and context-join metadata.
}
\description{
\code{add_context()} retrieves Cancer InFocus contextual variables for Census tract
GEOIDs in linked individual-level records and joins those variables back to
the records.
}
\details{
This function is useful when records already contain Census tract GEOIDs, or
when users want to run geographic linkage and contextual enrichment as
separate steps.
}
\examples{
\dontrun{
linked <- link_context(
  sample_addresses,
  address = address,
  state = "DC",
  geocoder = "census_single",
  confirm_external = TRUE
)

add_context(
  linked,
  tract_col = tract_geoid,
  measures = "Total Population"
)
}

}
\seealso{
\code{\link[=link_context]{link_context()}}, \code{\link[=get_context]{get_context()}}, \code{\link[=join_context]{join_context()}}
}
